services:
  webapp:
    image: "{{ webapp_image }}"
    container_name: "{{ webapp_container_name }}"
    restart: unless-stopped
    ports:
      - "{{ webapp_host_port }}:{{ webapp_container_port }}"
